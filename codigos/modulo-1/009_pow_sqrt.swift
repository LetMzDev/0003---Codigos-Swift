import UIKit

var x, y, z, resultado: Double

x = 2
y = 3
z = 100

resultado = pow ( x, y )
print ( "Resultado de elevar \(x) à \(y) é \(resultado)" )

resultado = sqrt ( z )
print ( "Resultado da raiz quadade de \(z) é \(resultado)" )
